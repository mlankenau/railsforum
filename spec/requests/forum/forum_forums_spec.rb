require 'spec_helper'

describe "Forum::Forums" do
  describe "GET /forum_forums" do
    it "works! (now write some real specs)" do
      get forum_forums_path
      expect(response.status).to be(200)
    end
  end
end
